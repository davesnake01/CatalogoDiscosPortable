# Dependencias de la app portable (Flet + SQLite).
# Fijadas: Flet rompe APIs entre versiones mayores (p. ej. page.set_clipboard
# desapareció en 0.80). Mantener flet y flet-desktop en la misma versión.
flet==0.85.2
flet-desktop==0.85.2
psutil

# Empaquetado (solo necesario para generar ejecutables).
# El workflow de GitHub Actions lo instala por separado.
pyinstaller
